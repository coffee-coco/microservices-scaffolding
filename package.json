{
  "name": "microservices-scaffolding",
  "version": "1.0.5",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "release": "standard-version && node sync-version.js",
    "develop": "git add . && git commit -m \"chore(release): publish new version\" && git push origin develop"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "express": "^4.21.2"
  },
  "devDependencies": {
    "standard-version": "^9.5.0"
  }
}
